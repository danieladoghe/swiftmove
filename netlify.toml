[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects Next.js and installs the official Next.js runtime
# (@netlify/plugin-nextjs), which handles SSR routes and the /api endpoints.
